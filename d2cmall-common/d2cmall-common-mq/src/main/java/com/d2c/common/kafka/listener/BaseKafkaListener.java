package com.d2c.common.kafka.listener;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class BaseKafkaListener {

    protected final Logger logger = LoggerFactory.getLogger(this.getClass());
    /**
     * @see ConsumerRecord
     */
}
